# Design: POS Sudo Guidance
**Status: Approved**

## Approach

Add a single bullet to `POS.md` documenting the sudo pattern. No code changes required. The reference implementation in `~/dev/install.py` is cited rather than reproduced.

## Tasks

1. ✓ Impl: Draft improved sudo helper code (sentinel, retry, encapsulated stdin) and add to `POS.md` as a guidance section with inline example
2. ✓ Process: confirm ready to archive
