# Proposal: Spec Directory
**Status: Superseded — incorporated into process-consolidation**

## Intent
A single `SPEC.md` becomes unwieldy for larger projects. A `SPEC/` directory should be an equally valid home for a specification, allowing it to be split across multiple files while preserving all existing behaviour — hierarchical placement, scoping rules, and the change management process.
