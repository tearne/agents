# Design: Clarify Note Status
**Status: Ready for Review**

## Approach
Two small edits to PROCESS.md:

1. The `> **Notes**` callout currently says "a brief `Intent` and an `Unresolved` section are all that is required." Replace this with wording that makes clear Notes are freeform — no headings required, just captured thoughts or discussion.

2. The proposal template shows the full heading structure under all statuses. Add a brief note above or within the template that the structure applies to Draft and beyond; a Note is freeform.

## Tasks
1. ✓ Impl: Update the `> **Notes**` callout to describe Notes as freeform, removing any implied mandatory sections
2. ✓ Impl: Add a sentence before the template clarifying that Notes are freeform and the template applies from Draft onwards
3. ✓ Process: Confirm ready to archive
