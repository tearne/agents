# Behaviour

## Git
Never execute git write commands (`git add`, `git commit`, `git push`, `git reset`, `git rm`, etc.) or suggest the user run them, unless explicitly instructed to do so.

## Markdown
Do not insert manual line breaks in prose. Write paragraphs as single unbroken lines so text reflows correctly with soft wrap.
